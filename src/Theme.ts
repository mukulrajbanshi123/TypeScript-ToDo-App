import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#05bcff",
    },
    secondary: {
      main: "#f44336",
    },
  },
});
export default theme;
